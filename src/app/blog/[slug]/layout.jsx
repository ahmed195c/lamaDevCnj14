const BlogLayout = ({children}) => {
    return(
        <div>
            <h2 className="bg-red-700">this is the blog layout</h2>
           
            {children}
        </div>
    )
}

export default BlogLayout